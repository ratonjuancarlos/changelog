var list = require('./promptTypes/list');
var inputNumber = require('./promptTypes/inputNumber');
var inputText = require('./promptTypes/inputText');

module.exports = {
    list: list,
    inputNumber: inputNumber,
    inputText: inputText,
}